import { ScholarDateTypePipe } from './scholar-date-type.pipe';

describe('ScholarDateTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ScholarDateTypePipe();
    expect(pipe).toBeTruthy();
  });
});
