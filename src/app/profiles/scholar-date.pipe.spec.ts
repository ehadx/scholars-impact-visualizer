import { ScholarDatePipe } from './scholar-date.pipe';

describe('ScholarDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ScholarDatePipe();
    expect(pipe).toBeTruthy();
  });
});
